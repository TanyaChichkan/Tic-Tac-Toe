export const addClassToElement = (el, className)=>el.classList.add(className);
export const removeClassFromElement = (el, className)=>el.classList.remove(className);
export const toggleClassOnElement = (el, className)=>el.classList.remove(className);